name: base
channels:
  - https://conda.anaconda.org/conda-forge
dependencies:
  - deepmerge
  - python=3.10
  - pip
  - pip:
      - tinydb_serialization
      - kubernetes-client
      - ansible-core==2.13.5
      - enoslib
      - zodb
  - icecream
  - bpython
  - gin-config
  - ansible-runner=2.3.2
  - ansible-core=2.13.5
  - pendulum
  - pydantic
  - invoke
  # - easydict
  # - sh
